shared_resources:
  - source: node_modules
    target: node_modules
    compatibility: package-lock.json

hooks:
  post_create: |
    echo "✓ Worktree created successfully!"
    echo "  Name: $HNHN_NAME"
    echo "  Path: $HNHN_PATH"
    echo "  Branch: $HNHN_BRANCH"
  pre_remove: |
    echo "Cleaning up worktree $HNHN_NAME..."
