// Docker Integration Tests - Phase 2
// Testing port allocation, compose generation, and container lifecycle

mod integration {
    pub mod port_allocation;
    pub mod docker_compose;
    pub mod docker_lifecycle;
}
